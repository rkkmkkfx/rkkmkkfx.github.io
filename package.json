{
  "name": "gh-pages",
  "author": "thierryc",
  "version": "1.0.2",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "rm -rf node_modules/.cache && rimraf out && next build && next export && touch out/.nojekyll && touch out/CNAME && echo \"rkkmkkfx.dev\" >> out/CNAME && gh-pages -d out"
  },
  "dependencies": {
    "next": "latest",
    "react": "^16.12.0",
    "react-dom": "^16.12.0"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.14.9",
    "@types/react": "^17.0.0",
    "babel-plugin-transform-define": "^1.3.2",
    "gh-pages": "^2.1.1",
    "rimraf": "^3.0.0",
    "typescript": "^4.1.2"
  }
}
